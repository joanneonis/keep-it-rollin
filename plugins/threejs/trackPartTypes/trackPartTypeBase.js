// base for trackparts

export class trackPartType {
  init () {
    // loads model
  }

  update () {
    // usually inside render animation loop
  }

  delete () {
    // unbinds everything
  }

  getModel () {
    // returns model
  }
}
